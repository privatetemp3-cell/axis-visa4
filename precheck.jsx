// Axis Visa — PreCheckModal stub (removed from MVP v1; retained for compatibility)
// PreCheckModal is no longer rendered in App() — the hero selector handles application starts.
Object.assign(window, { PreCheckModal: () => null });
